fetch('/cart/count/').then(response=>response.json()).then(data=>{const badge=document.querySelector('#cart-count');if(badge)badge.textContent=data.count}).catch(()=>{});
document.querySelector('.cart-button')?.addEventListener('click',()=>{window.location.href='/cart/'});
document.querySelector('.search-toggle')?.addEventListener('click',()=>document.querySelector('#featured')?.scrollIntoView({behavior:'smooth'}));
document.querySelectorAll('.filter-tabs button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filter-tabs button').forEach(b=>b.classList.remove('selected'));button.classList.add('selected');const label=button.textContent.trim();document.querySelectorAll('.product-card').forEach(card=>{card.style.display=label==='Featured'||(label==='New in'&&card.dataset.tag==='NEW')||(label==='Best sellers'&&(card.dataset.tag==='POPULAR'||card.dataset.tag==='FNS PICK'))?'':'none'})}));
document.querySelector('.newsletter button')?.addEventListener('click',()=>{const input=document.querySelector('#email');if(input.value.includes('@')){input.value='You’re on the list ✦';input.disabled=true}else{input.focus();input.placeholder='Enter a valid email'}});
